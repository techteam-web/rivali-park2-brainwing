import LocationsView from '../components/locations/LocationsView'

// /locations — reached from the "Location" tab in the home navbar. The page
// stays thin and delegates to components under components/locations.
const Locations = () => {
  return <LocationsView />
}

export default Locations
